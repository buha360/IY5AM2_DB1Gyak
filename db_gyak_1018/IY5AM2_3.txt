CREATE DATABASE IY5AM2;
USE IY5AM2;

CREATE TABLE TANFOLYAMOK
(tkod INT PRIMARY KEY,
ar INT CHECK(ar>0),
tipus CHAR(30),
megnevezes CHAR(30));

CREATE TABLE RESZTVEVO
(tajszam INT PRIMARY KEY,
nev CHAR(30) NOT NULL,
lakcím CHAR(40));

CREATE TABLE BEFIZETES
(tanfolyamok INT REFERENCES TANFOLYAM,
resztvevok INT REFERENCES RESZTVEVO,
befizetes INT);